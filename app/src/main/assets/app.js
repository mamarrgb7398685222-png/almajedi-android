const products=[
 {id:1,name:'Redmi Note 13 Pro+',spec:'512GB • RAM 12GB',price:590,status:'جديد',emoji:'📱'},
 {id:2,name:'Samsung S23 Ultra',spec:'512GB • RAM 12GB',price:2100,status:'مستخدم',emoji:'📲'},
 {id:3,name:'LT M50 Plus',spec:'256GB • RAM 8GB',price:550,status:'جديد',emoji:'📱'},
 {id:4,name:'شاحن سريع أصلي',spec:'قدرة 67W • ضمان',price:85,status:'جديد',emoji:'🔌'}
];
let cart=JSON.parse(localStorage.getItem('almajedi-cart')||'[]');let tab='home';
const screen=document.querySelector('#screen');
function money(n){return n.toLocaleString('ar-SA')+' ر.س'}
function save(){localStorage.setItem('almajedi-cart',JSON.stringify(cart));document.querySelector('#cartCount').textContent=cart.length}
function toast(t){const el=document.querySelector('#toast');el.textContent=t;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),1700)}
function productCard(p){return `<article class="product"><div class="product-art">${p.emoji}</div><span class="badge ${p.status==='مستخدم'?'used':''}">${p.status}</span><div class="product-body"><h3>${p.name}</h3><div class="spec">${p.spec}</div><div class="price">${money(p.price)}</div><button class="add" data-add="${p.id}">+</button></div></article>`}
function home(){return `<section class="page"><div class="welcome"><img class="welcome-logo" src="logo.png" alt=""><div class="welcome-copy"><h1>مرحباً بك في المجيدي 👋</h1><p>كل ما يحتاجه هاتفك في مكان واحد</p></div><span class="location">⌖ عدن - البريقة</span></div><label class="search"><input id="search" placeholder="ابحث عن جهاز أو إكسسوار"><span>⌕</span></label><article class="hero"><small>عروض خاصة 🔥</small><h2>قوة الأداء<br>بسعر أقوى</h2><p>أحدث الجوالات بضمان المجيدي</p><button class="primary" data-tab="categories">تسوّق الآن</button></article><div class="section-head"><h2>تسوق حسب القسم</h2><button data-tab="categories">عرض الكل ←</button></div><div class="categories">${[['📱','جوالات جديدة'],['♻️','جوالات مستخدمة'],['🎧','إكسسوارات'],['🛠️','الصيانة'],['💻','البرمجة'],['🔥','العروض']].map(x=>`<button class="category" data-cat="${x[1]}"><span>${x[0]}</span>${x[1]}</button>`).join('')}</div><div class="section-head"><h2>وصل حديثاً</h2><button data-tab="categories">عرض الكل ←</button></div><div class="products" id="products">${products.map(productCard).join('')}</div></section>`}
function categories(){return `<section class="page"><h1 class="title">الأقسام والمنتجات</h1><label class="search"><input id="search" placeholder="ابحث عن منتج"><span>⌕</span></label><div class="products" id="products">${products.map(productCard).join('')}</div><div class="section-head"><h2>خدمات المجيدي</h2></div><div class="card service"><span>🛠️</span><div><h3>طلب صيانة</h3><p>الشاشات، البطاريات، الشحن وتشخيص الأعطال</p></div></div><div class="card service"><span>💻</span><div><h3>خدمات البرمجة</h3><p>حلول برمجية ونقل بيانات وتحديثات</p></div></div></section>`}
function cartView(){let total=cart.reduce((s,id)=>s+products.find(p=>p.id===id).price,0);return `<section class="page"><h1 class="title">سلة المشتريات</h1>${cart.length?`<div class="list">${cart.map((id,i)=>{let p=products.find(x=>x.id===id);return `<div class="card cart-item"><div class="thumb">${p.emoji}</div><div><h3>${p.name}</h3><p>${money(p.price)}</p></div><button data-remove="${i}">حذف</button></div>`}).join('')}</div><div class="total"><span>الإجمالي</span><strong>${money(total)}</strong></div><button class="primary wide" id="whatsapp">إرسال الطلب عبر واتساب</button>`:`<div class="empty">🛒<strong>السلة فارغة</strong>أضف المنتجات التي تريد طلبها</div>`}</section>`}
function simple(title,body){return `<section class="page"><h1 class="title">${title}</h1><div class="empty">${body}</div></section>`}
function render(){document.querySelectorAll('.bottom-nav button').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));screen.innerHTML=tab==='home'?home():tab==='categories'?categories():tab==='cart'?cartView():tab==='orders'?simple('طلباتي','▤<strong>لا توجد طلبات بعد</strong>ستظهر طلبات الشراء والصيانة هنا'):simple('حسابي','♙<strong>أهلاً بك في المجيدي</strong>سجّل دخولك لاحقاً لمتابعة الطلبات والضمان');bind();save()}
function bind(){document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{tab=b.dataset.tab;render();scrollTo(0,0)});document.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>{cart.push(+b.dataset.add);save();toast('تمت الإضافة إلى السلة')});document.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{cart.splice(+b.dataset.remove,1);render()});document.querySelectorAll('[data-cat]').forEach(b=>b.onclick=()=>toast('تم اختيار قسم '+b.dataset.cat));const q=document.querySelector('#search');if(q)q.oninput=()=>{const list=document.querySelector('#products');list.innerHTML=products.filter(p=>(p.name+' '+p.spec).toLowerCase().includes(q.value.toLowerCase())).map(productCard).join('');bind()};const w=document.querySelector('#whatsapp');if(w)w.onclick=()=>{const msg='مرحباً، أريد طلب:\n'+cart.map(id=>{let p=products.find(x=>x.id===id);return '• '+p.name+' - '+money(p.price)}).join('\n');location.href='https://wa.me/967771358835?text='+encodeURIComponent(msg)}}
document.querySelectorAll('.bottom-nav button').forEach(b=>b.onclick=()=>{tab=b.dataset.tab;render()});document.querySelectorAll('.icon-btn').forEach(b=>b.onclick=()=>toast(b.dataset.action==='menu'?'القائمة الرئيسية':'لا توجد إشعارات جديدة'));if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js');render();
